#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * argstostr - check the code for ALX School students.
 * @ac : input
 * @av : input
 * Return: Always 0.
 */

char *argstostr(int ac, char **av)
{
	char *ptr;
	if (ac == 0 || av == NULL)
		return (NULL);
	ptr = (char *)malloc(sizeof(*av) * );
}
