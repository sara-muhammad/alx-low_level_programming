#include"main.h"

/**
 * _isupper - check the code.
 *
 * @c : function input
 *
 * Return: 1(uppercase).
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
